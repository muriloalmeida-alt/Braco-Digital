# Só necessário em builds de produção (Railway, etc.). Em dev, o proxy do
# Vite (vite.config.ts) já resolve /api -> http://localhost:3001.
# Aponte para a URL pública do serviço apps/api, sem "/api" no final.
VITE_API_BASE_URL=https://braco-api.up.railway.app
